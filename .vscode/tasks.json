{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Portfolio git push",
      "type": "shell",
      "command": "git add -A && git commit -m 'feat: initial producer portfolio -- 6 wineries, 21 wines, /wines/[slug] detail pages, portfolio on producers/products/regions pages' && git push origin main",
      "options": {
        "cwd": "/workspaces/Terra-Trionfo"
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
