{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Wine card filters push",
      "type": "shell",
      "command": "git add -A && git commit -m 'feat: wine type filters + Add to Inquiry on all wine cards -- WineCard/AddToCartButton components, type/collection dual-filter on /products, cards on homepage and wine detail pages' && git push origin main",
      "options": {
        "cwd": "/workspaces/Terra-Trionfo"
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
