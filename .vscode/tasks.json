{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Portfolio visibility fix",
      "type": "shell",
      "command": "git add -A && git commit -m 'fix: portfolio now visible on homepage, /products, and /producers -- replace empty states with live data' && git push origin main",
      "options": {
        "cwd": "/workspaces/Terra-Trionfo"
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
